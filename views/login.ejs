<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Connexion LDAP</title>
</head>
<body>
    <h1>Connexion à l'annuaire LDAP</h1>
    <form action="/login" method="post">
        <input type="text" name="uri" placeholder="URI de la base LDAP" value="<%= uri %>" required><br>
        <input type="text" name="dn" placeholder="DN" value="<%= dn %>" required><br>
        <input type="password" name="password" placeholder="Mot de passe" required><br>
        <input type="text" name="dc" placeholder="Racine DC" value="<%= dc %>" required><br>
        <button type="submit">Se connecter</button>
    </form>
    
    <% if (error) { %>
        <p style="color: red;"><%= error %></p>
    <% } %>
</body>
</html>
